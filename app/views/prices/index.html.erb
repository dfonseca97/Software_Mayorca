<p id="notice"><%= notice %></p>

<h1>Tarifas</h1>

<table>
  <thead>
    <tr>
      <th>Posición</th>
      <th>Actividad</th>
      <th>Tarifa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @prices.each do |price| %>
      <tr>
        <td><%= price.position %></td>
        <td><%= price.activity %></td>
        <td><%= price.price %></td>
        <td><%= link_to 'Mostrar', price %></td>
        <td><%= link_to 'Modificar', edit_price_path(price) %></td>
        <td><%= link_to 'Eliminar', price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Tarifa', new_price_path %>
