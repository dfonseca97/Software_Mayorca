
<body class="background_image">
<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
  <div class="field">
    <%= form.label :concept %>
    <%= form.text_field :concept, id: :expense_concept %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :expense_amount %>
  </div>

  <div class="field">
    <%= form.label :receiver %>
    <%= form.text_field :receiver, id: :expense_receiver %>
  </div>

  <div class="actions">
    <%= form.submit 'Agregar Egreso' , class: 'btn btn-success btn-lg btn-block'%>
  </div>
<% end %>
</div>

</body>